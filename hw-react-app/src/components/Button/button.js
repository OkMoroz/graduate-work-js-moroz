import React from "react";
import "./button.css";

const Button = () => {
  return (
    <div>
      <button className="btn" type="submit">
        Login
      </button>
    </div>
  );
};

export default Button;
